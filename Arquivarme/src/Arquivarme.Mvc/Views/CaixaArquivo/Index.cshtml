@model IEnumerable<Arquivarme.Dominio.Models.CaixaArquivo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Entrege)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataCriacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Empresa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Entrege)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataCriacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa.Bairro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CaixaArquivoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CaixaArquivoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CaixaArquivoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
